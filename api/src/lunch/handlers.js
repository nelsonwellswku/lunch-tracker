const getLunch = (req, res) => {
  res.json({
    success: true,
  });
};

const createLunch = (req, res) => {
  res.json({
    success: true,
  });
};

module.exports = {
  getLunch,
  createLunch,
};
